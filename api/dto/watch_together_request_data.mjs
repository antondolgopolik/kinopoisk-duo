export class WatchTogetherRequestData {
    constructor(movieId, movieTitle, userId, isActive) {
        this.movieId = movieId;
        this.movieTitle = movieTitle;
        this.userId = userId;
        this.isActive = isActive;
    }
}